general {
 output_format = "i3bar"
 colors = true
 interval = 30
}

order += "load"
order += "battery 0"
order += "time"

load {
 format = "%5min"
}

time {
 format = "%a %d %H:%M"
}

battery 0 {
 format = "%status %percentage"
 format_down = "No battery"
 status_chr = "charging"
 status_bat = "battery"
 status_full = "⚡"
 path = "/sys/class/power_supply/BAT%d/uevent"
 low_threshold = 10
}
